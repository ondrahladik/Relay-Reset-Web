# Relay-Reset-Web

## Config
```php
$NAME_RELAY = 'Relay Reset';  # Name of relay
$RESPONSE_TEXT = "Command executed"; # Response text
$RESPONSE_ERROR = "Server error"; # Response error
$RESPONSE_ACTIVE = true;  # Response active
$RESET_DEVICE = true;  # Restart device button
$RESET_ALL = true;  # Restart all button
$ON_OFF_RELAY = true;  # ON/OFF relay button

$MQTT_BROKER = '';  # MQTT broker
$MQTT_PORT = 1883;  # MQTT port
$MQTT_TOPIC = '';  # MQTT topic

# If you have MQTT authentication
$MQTT_USERNAME = ''; # MQTT username
$MQTT_PASSWORD = ''; # MQTT password
```